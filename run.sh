cmake . && make && sudo ./helloWorldC__

# while : ; do
#     cmake .
#     make
#     sudo ./helloWorldC__ &
#     runPid=$!
#     read -p 'enter to re-run; q to quit ' input
#     kill -KILL $runPid
#     [[ $input -eq 'q' ]] && break
# done
